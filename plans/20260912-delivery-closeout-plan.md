# Nuwax 客户端提测收尾

状态：已实施，双平台测试包已产出并完成安装/启动态验收（证据与未验证项见同名 `20260912-delivery-closeout-acceptance.md`）。用户已确认：全部业务服务跟随登录起停、商业版设备身份独立、macOS + Windows 真机验收（SSH win-pc）。

主进程统一注册和启停，以可撤销的会话代次阻止旧请求写回；商业版不导入任何历史产品数据。换域清认证及浏览器站点存储，停止完成后重指网关。文件另存采用临时文件和原子落盘。启动屏首次可见后至少 800ms，初始化失败可重试。

门禁：回归测试、隔离社区基线、商业测试/类型/构建、双平台安装包真实业务闭环。完整证据记录于同名 acceptance 文档；未验证不标为通过。正式更新渠道不变。

实施前 HEAD 和工作树 patch（含未跟踪文件）已备份至 /tmp/nuwax-delivery-baseline-20260912-101438；初始 overlay:check 一致，专项 5 files / 44 tests passed。

## 实机发现后补齐

- 未登录仍需独立初始化页面网关，避免从 ComputerServer 启动路径移除后断链。新安装默认随包前端（gateway），保留已有加载模式偏好。
- 设置页域名修改复用企业登录事务；禁用商业版启动期旧 quickInit 凭据覆盖。
- 商业版禁止按端口终止未知进程；停止失败保留进程引用以支持重试。
- 注册接口真实首登仅 Bearer 返回 4000（动态认证码或密码不能为空）；等待后端接口支持，不使用旧 savedKey 绕过独立设备注册。
